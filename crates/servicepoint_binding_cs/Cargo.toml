[package]
name = "servicepoint_binding_cs"
version = "0.4.2"
edition = "2021"
publish = false
readme = "../../README.md"

[lib]
crate-type = ["cdylib"]

[dependencies]
servicepoint = { path = "../servicepoint", features = ["c_api"] }

[build-dependencies]
csbindgen = "1.8.0"
